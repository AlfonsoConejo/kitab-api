import { Router } from "express";
import { } from "../classes/classes.controller.js";
import { authMiddleware } from "../../middleware/authMiddleware.js";

const router = Router();


  
export default router;